const initialState = {
    openModal: "",
};

function fileReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default fileReducer;